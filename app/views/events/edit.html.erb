<h1>Editing post</h1>
 
<%= render 'form' %>

<h4>Invites:</h4>
<% @event.invites.each do |comment| %>
<p>
<strong><%= invite.user %>: </strong><%= invite.response %>
<%= link_to 'Destroy Invite', [invite.event.post, invite], 
  method: :delete,
  data: { confirm: 'Are you sure?' } %>
</p>
<% end %>
 
<%= link_to 'Back', event_path(@pevent), class: "btn"  %> <%= link_to 'Delete', event_path(@event),
        method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn' %>